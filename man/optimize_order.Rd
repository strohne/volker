% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{optimize_order}
\alias{optimize_order}
\title{Helper function: optimize the ordering of items for profile plots}
\usage{
optimize_order(data, method = TRUE)
}
\arguments{
\item{data}{A dataframe with the columns \code{item}, \code{value}, and \code{.cross}.}

\item{method}{The ordering method. Either \code{TRUE} to automatically select a
method (\code{"olo"} if \pkg{seriation} is installed, otherwise \code{"min"}), or
one of the character values \code{"max"}, \code{"min"}, \code{"spread"}, \code{"gw"}, or
\code{"olo"}. Defaults to \code{TRUE}.}
}
\value{
A vector of \code{item} values in the optimized order.
}
\description{
Determines an ordering of the \code{item} factor levels so that grouped line
charts (e.g. cluster profile plots) read clearly, with as little line
"jumping" / crossing as possible. The returned vector can be passed to
\code{\link[=.plot_lines]{.plot_lines()}} (or used directly to relevel the \code{item} factor).
}
\details{
The following methods are supported:
\describe{
\item{\code{TRUE}}{Automatically select a method (default). Uses \code{"olo"} if
the \pkg{seriation} package is available, otherwise falls back to
\code{"min"}.}
\item{\code{"max"}}{Order items by descending grand mean of \code{value}
(default). Produces a monotonic ladder.}
\item{\code{"min"}}{Order items by ascending grand mean of \code{value}.}
\item{\code{"spread"}}{Order items by the range (\code{max - min}) of \code{value}
across groups. Highlights the items that discriminate
the groups most.}
\item{\code{"gw"}, \code{"olo"}}{Use seriation to minimise crossings across all
groups simultaneously. \code{"gw"} = Gruvaeus–Wainer, \code{"olo"} = optimal
leaf ordering. Requires the \pkg{seriation} package.}
}
}
\keyword{internal}
