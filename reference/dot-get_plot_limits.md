# Guess plot limits from values

Guess plot limits from values

## Usage

``` r
.get_plot_limits(values)
```

## Arguments

- values:

  Numeric values vector

## Value

Either a vector c(0,1) or c(-1,1) or the range of the values,, whatever
covers the values better
