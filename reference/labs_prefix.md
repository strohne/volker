# Remove common prefix from the first column

Remove common prefix from the first column

## Usage

``` r
labs_prefix(result)
```

## Arguments

- result:

  A tibble with item names in the first column

## Value

A tibble with the first column renamed to the prefix and the prefix
removed from column values.
