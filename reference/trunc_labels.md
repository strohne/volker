# Truncate labels

Truncate labels that exceed a specified maximum length.

## Usage

``` r
trunc_labels(x, max_length = 20)
```

## Arguments

- x:

  A character vector.

- max_length:

  Maximum length, default is 20. The ellipsis "..." is appended to
  shortened labels.

## Value

A character vector with truncated labels.
