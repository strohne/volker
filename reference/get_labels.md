# Get the labels of values from a codebook

Get the labels of values from a codebook

## Usage

``` r
get_labels(codes, values)
```

## Arguments

- codes:

  The codebook as it results from the codebook() function

- values:

  A vector of labels

## Value

The labels. If the values are not present in the codebook, returns the
values.
